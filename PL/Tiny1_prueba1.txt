EstoEsUnaVariable
EstaEsLaNum2
"Esto es una cadena"
# Esto es un comentario
	5 5.5 && ; = ( ) + - * /
	< > <= >= == != % [ ] { }
	. -> , & 
INT, REAL, BOOL, TRUE, FALSE, AND, OR, NOT, STRING, NULL, PROC, IF,

instrucciones{ 
	 read dividendo; 
	 read divisor; 
	 cociente = dividendo / divisor; 
	 resto = dividendo % divisor; 
	 divisible = resto == 0; 
	 write cociente; 
	 write resto; 
	 write divisible 
}


typedef struct {
	int x; int y;
} tpunto;
 
typedef struct {
	int tam; tpunto* puntos[10];
} tlistapuntos; 

tlistapuntos lista; 

int i; 
i=0; 
while (i<5) { 
	 tpunto* punto; 
	 punto = new tpunto; 
	 lista.puntos[i] = punto; 
	 { 
	 int punto; 
	 punto =2*i; #1
	 lista.puntos[i]->x = punto; 
	 lista.puntos[i]->y = lista.puntos[lista.puntos[i]->x/2]->y + 1; #2
	 i=i+1; 
	 } 
} 

lista.tam=5;